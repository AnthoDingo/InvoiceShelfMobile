<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:InvoiceShelf.ViewModels.Estimates"
             xmlns:models="clr-namespace:InvoiceShelf.Models.Admin"
             xmlns:helpers="clr-namespace:InvoiceShelf.Helpers"
             x:Class="InvoiceShelf.Views.Estimates.EstimateDetailPage"
             x:DataType="viewmodels:EstimateDetailViewModel"
             Title="{Binding Estimate.EstimateNumber}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <helpers:InverseBooleanConverter x:Key="InverseBoolConverter"/>
            <helpers:IsNotNullConverter     x:Key="IsNotNullConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>

        <!-- Chargement -->
        <VerticalStackLayout
            VerticalOptions="Center"
            HorizontalOptions="Center"
            Spacing="14"
            IsVisible="{Binding IsLoading}">
            <ActivityIndicator IsRunning="True" Color="{StaticResource Primary}" WidthRequest="36" HeightRequest="36"/>
            <Label Text="Chargement…" TextColor="{StaticResource Gray400}" HorizontalOptions="Center" FontSize="14"/>
        </VerticalStackLayout>

        <!-- Contenu -->
        <ScrollView IsVisible="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}">
            <VerticalStackLayout Spacing="0" Padding="0,0,0,48">

                <!-- En-tête coloré -->
                <Border
                    Margin="16,16,16,0"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 16"
                    BackgroundColor="{StaticResource Primary}"
                    Padding="20,22">
                    <VerticalStackLayout Spacing="10">

                        <Grid ColumnDefinitions="*,Auto">
                            <Label
                                Grid.Column="0"
                                Text="{Binding Estimate.EstimateNumber}"
                                TextColor="White"
                                FontSize="20"
                                FontAttributes="Bold"/>
                            <Border
                                Grid.Column="1"
                                StrokeShape="RoundRectangle 6"
                                StrokeThickness="1.5"
                                Stroke="White"
                                BackgroundColor="Transparent"
                                Padding="10,4">
                                <Label
                                    Text="{Binding Estimate.FormattedStatus}"
                                    TextColor="White"
                                    FontAttributes="Bold"
                                    FontSize="12"/>
                            </Border>
                        </Grid>

                        <Label
                            Text="{Binding Estimate.FormattedTotal}"
                            TextColor="White"
                            FontSize="38"
                            FontAttributes="Bold"/>

                        <Grid ColumnDefinitions="*,*" Margin="0,6,0,0">
                            <VerticalStackLayout Grid.Column="0" Spacing="3">
                                <Label Text="Date du devis" TextColor="White" Opacity="0.65" FontSize="11"/>
                                <Label Text="{Binding Estimate.FormattedEstimateDate}" TextColor="White" FontSize="13"/>
                            </VerticalStackLayout>
                            <VerticalStackLayout Grid.Column="1" Spacing="3">
                                <Label Text="Date d'expiration" TextColor="White" Opacity="0.65" FontSize="11"/>
                                <Label Text="{Binding Estimate.FormattedExpiryDate}" TextColor="White" FontSize="13"/>
                            </VerticalStackLayout>
                        </Grid>

                    </VerticalStackLayout>
                </Border>

                <!-- Client -->
                <Border
                    Margin="16,12,16,0"
                    StrokeThickness="1"
                    Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
                    StrokeShape="RoundRectangle 14"
                    BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                    Padding="16">
                    <VerticalStackLayout Spacing="6">
                        <Label Text="CLIENT" FontSize="11" FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"
                               CharacterSpacing="1"/>
                        <Label Text="{Binding Estimate.Customer.Name}" FontSize="16" FontAttributes="Bold"/>
                        <Label Text="{Binding Estimate.Customer.Email}" FontSize="13"
                               TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}"
                               IsVisible="{Binding Estimate.Customer.Email, Converter={StaticResource IsNotNullConverter}}"/>
                        <Label Text="{Binding Estimate.Customer.Phone}" FontSize="13"
                               TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}"
                               IsVisible="{Binding Estimate.Customer.Phone, Converter={StaticResource IsNotNullConverter}}"/>
                    </VerticalStackLayout>
                </Border>

                <!-- Articles -->
                <Border
                    Margin="16,12,16,0"
                    StrokeThickness="1"
                    Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
                    StrokeShape="RoundRectangle 14"
                    BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                    Padding="16"
                    IsVisible="{Binding HasItems}">
                    <VerticalStackLayout Spacing="0">
                        <Label Text="ARTICLES" FontSize="11" FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"
                               CharacterSpacing="1"
                               Margin="0,0,0,10"/>
                        <CollectionView ItemsSource="{Binding Estimate.Items}">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="models:Item">
                                    <Grid ColumnDefinitions="*,Auto" Padding="0,8">
                                        <VerticalStackLayout Grid.Column="0" Spacing="2">
                                            <Label Text="{Binding Name}" FontSize="14" FontAttributes="Bold"/>
                                            <Label Text="{Binding QuantityUnit}" FontSize="12"
                                                   TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"/>
                                        </VerticalStackLayout>
                                        <Label Grid.Column="1" Text="{Binding FormattedTotal}"
                                               FontSize="14" FontAttributes="Bold"
                                               HorizontalTextAlignment="End" VerticalOptions="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Border>

                <!-- Récapitulatif financier -->
                <Border
                    Margin="16,12,16,0"
                    StrokeThickness="1"
                    Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
                    StrokeShape="RoundRectangle 14"
                    BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                    Padding="16">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="RÉCAPITULATIF" FontSize="11" FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"
                               CharacterSpacing="1"/>

                        <Grid ColumnDefinitions="*,Auto">
                            <Label Grid.Column="0" Text="Sous-total" FontSize="14"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"/>
                            <Label Grid.Column="1" Text="{Binding Estimate.FormattedSubTotal}" FontSize="14" HorizontalTextAlignment="End"/>
                        </Grid>
                        <Grid ColumnDefinitions="*,Auto">
                            <Label Grid.Column="0" Text="Taxes" FontSize="14"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"/>
                            <Label Grid.Column="1" Text="{Binding Estimate.FormattedTax}" FontSize="14" HorizontalTextAlignment="End"/>
                        </Grid>
                        <Grid ColumnDefinitions="*,Auto">
                            <Label Grid.Column="0" Text="Remise" FontSize="14"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"/>
                            <Label Grid.Column="1" Text="{Binding Estimate.FormattedDiscount}" FontSize="14"
                                   TextColor="{StaticResource StatusCompleted}" HorizontalTextAlignment="End"/>
                        </Grid>

                        <BoxView HeightRequest="1"
                                 BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"/>

                        <Grid ColumnDefinitions="*,Auto">
                            <Label Grid.Column="0" Text="Total" FontSize="17" FontAttributes="Bold"/>
                            <Label Grid.Column="1" Text="{Binding Estimate.FormattedTotal}"
                                   FontSize="17" FontAttributes="Bold"
                                   TextColor="{StaticResource Primary}" HorizontalTextAlignment="End"/>
                        </Grid>
                    </VerticalStackLayout>
                </Border>

                <!-- Notes -->
                <Border
                    Margin="16,12,16,0"
                    StrokeThickness="1"
                    Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
                    StrokeShape="RoundRectangle 14"
                    BackgroundColor="{AppThemeBinding Light={StaticResource Gray50}, Dark={StaticResource Gray900}}"
                    Padding="16"
                    IsVisible="{Binding HasNotes}">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="NOTES" FontSize="11" FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"
                               CharacterSpacing="1"/>
                        <Label Text="{Binding Estimate.Notes}" FontSize="14"
                               TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}"
                               LineBreakMode="WordWrap"/>
                    </VerticalStackLayout>
                </Border>

            </VerticalStackLayout>
        </ScrollView>

    </Grid>
</ContentPage>
