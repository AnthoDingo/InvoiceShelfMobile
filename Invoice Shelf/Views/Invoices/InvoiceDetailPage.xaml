<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:InvoiceShelf.ViewModels.Invoices"
             xmlns:models="clr-namespace:InvoiceShelf.Models.Admin"
             xmlns:helpers="clr-namespace:InvoiceShelf.Helpers"
             x:Class="InvoiceShelf.Views.Invoices.InvoiceDetailPage"
             xmlns:loc="clr-namespace:InvoiceShelf.Localization"
             x:DataType="viewmodels:InvoiceDetailViewModel"
             Title="{Binding Invoice.InvoiceNumber}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <helpers:InverseBooleanConverter x:Key="InverseBoolConverter"/>
            <helpers:IsNotNullConverter     x:Key="IsNotNullConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid Style="{StaticResource ResponsiveContainer}">

        <!-- Chargement -->
        <VerticalStackLayout
            VerticalOptions="Center"
            HorizontalOptions="Center"
            Spacing="14"
            IsVisible="{Binding IsLoading}">
            <ActivityIndicator IsRunning="True" Color="{StaticResource Primary}" WidthRequest="36" HeightRequest="36"/>
            <Label Text="{loc:Translate Common_Loading}" TextColor="{StaticResource Gray400}" HorizontalOptions="Center" FontSize="14"/>
        </VerticalStackLayout>

        <!-- Contenu -->
        <ScrollView IsVisible="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}">
            <VerticalStackLayout Spacing="0" Padding="0,0,0,48">

                <!-- En-tête coloré -->
                <Border
                    Margin="16,16,16,0"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 16"
                    BackgroundColor="{StaticResource Primary}"
                    Padding="20,22">
                    <VerticalStackLayout Spacing="10">

                        <Grid ColumnDefinitions="*,Auto">
                            <Label
                                Grid.Column="0"
                                Text="{Binding Invoice.InvoiceNumber}"
                                TextColor="White"
                                FontSize="20"
                                FontAttributes="Bold"/>
                            <Border
                                Grid.Column="1"
                                StrokeShape="RoundRectangle 6"
                                StrokeThickness="1.5"
                                Stroke="White"
                                BackgroundColor="Transparent"
                                Padding="10,4">
                                <Label
                                    Text="{Binding Invoice.FormattedStatus}"
                                    TextColor="White"
                                    FontAttributes="Bold"
                                    FontSize="12"/>
                            </Border>
                        </Grid>

                        <Label
                            Text="{Binding Invoice.FormattedTotal}"
                            TextColor="White"
                            FontSize="38"
                            FontAttributes="Bold"/>

                        <Grid ColumnDefinitions="*,*" Margin="0,6,0,0">
                            <VerticalStackLayout Grid.Column="0" Spacing="3">
                                <Label Text="{loc:Translate Invoice_InvoiceDate}" TextColor="White" Opacity="0.65" FontSize="11"/>
                                <Label Text="{Binding Invoice.FormattedInvoiceDate}" TextColor="White" FontSize="13"/>
                            </VerticalStackLayout>
                            <VerticalStackLayout Grid.Column="1" Spacing="3">
                                <Label Text="{loc:Translate Invoice_DueDate}" TextColor="White" Opacity="0.65" FontSize="11"/>
                                <Label Text="{Binding Invoice.FormattedDueDate}" TextColor="White" FontSize="13"/>
                            </VerticalStackLayout>
                        </Grid>

                        <!-- Progression jusqu'à l'échéance : verte de 0 à 33%,
                             orange de 33 à 66%, rouge de 66 à 100% du temps
                             écoulé entre la date de facture et la date
                             d'échéance. -->
                        <VerticalStackLayout
                            Spacing="6"
                            Margin="0,4,0,0"
                            IsVisible="{Binding Invoice.ShowDueProgress}">
                            <Grid ColumnDefinitions="*,Auto">
                                <Label
                                    Grid.Column="0"
                                    Text="{Binding Invoice.FormattedDueAmount, StringFormat={loc:Translate Invoice_DueBalanceFormat}}"
                                    TextColor="White"
                                    Opacity="0.75"
                                    FontSize="12"
                                    IsVisible="{Binding Invoice.HasDueAmount}"/>
                                <Label
                                    Grid.Column="1"
                                    Text="{Binding Invoice.DueProgressLabel}"
                                    TextColor="White"
                                    Opacity="0.85"
                                    FontAttributes="Bold"
                                    FontSize="12"/>
                            </Grid>
                            <ProgressBar
                                Progress="{Binding Invoice.DueProgress}"
                                ProgressColor="{Binding Invoice.DueProgressColor}"
                                BackgroundColor="#33FFFFFF"
                                HeightRequest="8"/>
                        </VerticalStackLayout>

                        <!-- Enregistrer un paiement (facture envoyée ou en retard) :
                             ouvre RecordPaymentPage avec cette facture pré-remplie. -->
                        <Button
                            Text="{loc:Translate RecordPayment_Title}"
                            FontSize="14"
                            FontAttributes="Bold"
                            BackgroundColor="White"
                            TextColor="{StaticResource Primary}"
                            CornerRadius="10"
                            HeightRequest="44"
                            Margin="0,6,0,0"
                            Command="{Binding RecordPaymentCommand}"
                            IsVisible="{Binding CanRecordPayment}"/>

                    </VerticalStackLayout>
                </Border>

                <!-- Client -->
                <Border
                    Margin="16,12,16,0"
                    StrokeThickness="1"
                    Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
                    StrokeShape="RoundRectangle 14"
                    BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                    Padding="16">
                    <VerticalStackLayout Spacing="6">
                        <Label Text="{loc:Translate Common_CustomerSectionTitle}" FontSize="11" FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"
                               CharacterSpacing="1"/>
                        <Label Text="{Binding Invoice.Customer.Name}" FontSize="16" FontAttributes="Bold"/>
                        <Label Text="{Binding Invoice.Customer.Email}" FontSize="13"
                               TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}"
                               IsVisible="{Binding Invoice.Customer.Email, Converter={StaticResource IsNotNullConverter}}"/>
                        <Label Text="{Binding Invoice.Customer.Phone}" FontSize="13"
                               TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}"
                               IsVisible="{Binding Invoice.Customer.Phone, Converter={StaticResource IsNotNullConverter}}"/>
                    </VerticalStackLayout>
                </Border>

                <!-- Articles -->
                <Border
                    Margin="16,12,16,0"
                    StrokeThickness="1"
                    Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
                    StrokeShape="RoundRectangle 14"
                    BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                    Padding="16"
                    IsVisible="{Binding HasItems}">
                    <VerticalStackLayout Spacing="0">
                        <Label Text="{loc:Translate Common_ItemsSectionTitle}" FontSize="11" FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"
                               CharacterSpacing="1"
                               Margin="0,0,0,10"/>
                        <CollectionView ItemsSource="{Binding Invoice.Items}">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="models:Item">
                                    <Grid ColumnDefinitions="*,Auto" Padding="0,8">
                                        <VerticalStackLayout Grid.Column="0" Spacing="2">
                                            <Label Text="{Binding Name}" FontSize="14" FontAttributes="Bold"/>
                                            <Label Text="{Binding QuantityUnit}" FontSize="12"
                                                   TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"/>
                                        </VerticalStackLayout>
                                        <Label Grid.Column="1" Text="{Binding FormattedTotal}"
                                               FontSize="14" FontAttributes="Bold"
                                               HorizontalTextAlignment="End" VerticalOptions="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Border>

                <!-- Récapitulatif financier -->
                <Border
                    Margin="16,12,16,0"
                    StrokeThickness="1"
                    Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
                    StrokeShape="RoundRectangle 14"
                    BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                    Padding="16">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="{loc:Translate Common_SummarySectionTitle}" FontSize="11" FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"
                               CharacterSpacing="1"/>

                        <Grid ColumnDefinitions="*,Auto">
                            <Label Grid.Column="0" Text="{loc:Translate Common_SubTotal}" FontSize="14"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"/>
                            <Label Grid.Column="1" Text="{Binding Invoice.FormattedSubTotal}" FontSize="14" HorizontalTextAlignment="End"/>
                        </Grid>
                        <Grid ColumnDefinitions="*,Auto">
                            <Label Grid.Column="0" Text="{loc:Translate Common_Tax}" FontSize="14"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"/>
                            <Label Grid.Column="1" Text="{Binding Invoice.FormattedTax}" FontSize="14" HorizontalTextAlignment="End"/>
                        </Grid>
                        <Grid ColumnDefinitions="*,Auto">
                            <Label Grid.Column="0" Text="{loc:Translate Common_Discount}" FontSize="14"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"/>
                            <Label Grid.Column="1" Text="{Binding Invoice.FormattedDiscount}" FontSize="14"
                                   TextColor="{StaticResource StatusCompleted}" HorizontalTextAlignment="End"/>
                        </Grid>

                        <BoxView HeightRequest="1"
                                 BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"/>

                        <Grid ColumnDefinitions="*,Auto">
                            <Label Grid.Column="0" Text="{loc:Translate Common_Total}" FontSize="17" FontAttributes="Bold"/>
                            <Label Grid.Column="1" Text="{Binding Invoice.FormattedTotal}"
                                   FontSize="17" FontAttributes="Bold"
                                   TextColor="{StaticResource Primary}" HorizontalTextAlignment="End"/>
                        </Grid>

                        <Grid ColumnDefinitions="*,Auto">
                            <Label Grid.Column="0" Text="{loc:Translate Common_DueBalanceLabel}" FontSize="14"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"/>
                            <Label Grid.Column="1" Text="{Binding Invoice.FormattedDueAmount}"
                                   FontSize="14" FontAttributes="Bold"
                                   TextColor="{StaticResource StatusOverdue}" HorizontalTextAlignment="End"/>
                        </Grid>
                    </VerticalStackLayout>
                </Border>

                <!-- Enregistrer un paiement (visible si solde dû > 0) -->
                <!--<Button
                    Margin="16,12,16,0"
                    Text="Enregistrer un paiement"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="White"
                    Command="{Binding RecordPaymentCommand}"
                    IsVisible="{Binding CanRecordPayment}"/>-->

                <!-- Modifier (visible uniquement sur une facture en brouillon) -->
                <Button
                    Margin="16,12,16,0"
                    Text="{loc:Translate Common_Edit}"
                    BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray700}}"
                    TextColor="{AppThemeBinding Light={StaticResource Gray700}, Dark={StaticResource White}}"
                    Command="{Binding EditInvoiceCommand}"
                    IsVisible="{Binding IsDraft}"/>

                <!-- Notes -->
                <Border
                    Margin="16,12,16,0"
                    StrokeThickness="1"
                    Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
                    StrokeShape="RoundRectangle 14"
                    BackgroundColor="{AppThemeBinding Light={StaticResource Gray50}, Dark={StaticResource Gray900}}"
                    Padding="16"
                    IsVisible="{Binding HasNotes}">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="{loc:Translate Common_NotesSectionTitle}" FontSize="11" FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"
                               CharacterSpacing="1"/>
                        <Label Text="{Binding Invoice.Notes}" FontSize="14"
                               TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}"
                               LineBreakMode="WordWrap"/>
                    </VerticalStackLayout>
                </Border>

            </VerticalStackLayout>
        </ScrollView>

    </Grid>
</ContentPage>
